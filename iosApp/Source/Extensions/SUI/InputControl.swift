//
//  InputControl.swift
//  iosApp
//
//  Created by Чаусов Николай on 09.06.2023.
//  Copyright © 2023 MobileUp. All rights reserved.
//

import SwiftUI

// TODO: https://ascnt.atlassian.net/browse/UPUP-616
/* https://ascnt.atlassian.net/browse/UPUP-616
extension Kmm_form_validationKeyboardType {
    func toKeyboardType() -> UIKeyboardType {
        switch self {
        case .ascii:
            return .asciiCapable
        case .number:
            return .numberPad
        case .phone:
            return .phonePad
        case .uri:
            return .URL
        case .email:
            return .emailAddress
        case .numberpassword:
            return .numberPad
        case .text, .password:
            return .default
        default:
            return .default
        }
    }
}

extension Kmm_form_validationImeAction {
    func toSubmitLabel() -> SubmitLabel {
        switch self {
        case .done:
            return .done
        case .go:
            return .go
        case .search:
            return .search
        case .send:
            return .send
        case .previous:
            return .route
        case .next:
            return .next
        default:
            return .done
        }
    }
}

extension Kmm_form_validationKeyboardCapitalization {
    func toTextInputAutocapitalization() -> TextInputAutocapitalization {
        switch self {
        case .characters:
            return .characters
        case .sentences:
            return .sentences
        case .words:
            return .words
        case .none:
            return .never
        default:
            return .never
        }
    }
}
*/